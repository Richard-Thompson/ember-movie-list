{{#each movies as |movie|}}
    <div class='movie'>
        <img src={{movie.image}}/>
        <div class='details'>
            <h1>{{movie.title}}</h1>
            <p>{{movie.description}}</p>
        </div>
    </div>
{{/each}}
